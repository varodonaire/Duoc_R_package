% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generation.R
\name{generate_left_tail_data_ln}
\alias{generate_left_tail_data_ln}
\title{Generate Left Tail Data Based on Inverted Log-Normal Distribution}
\usage{
generate_left_tail_data_ln(n, mean, sd, min_val, max_val)
}
\arguments{
\item{n}{The number of data points to generate. Must be a positive integer.}

\item{mean}{The mean of the original log-normal distribution before inversion and scaling. 
Must be a positive number.}

\item{sd}{The standard deviation of the original log-normal distribution before inversion and scaling. 
Must be a positive number.}

\item{min_val}{The minimum value in the scaled data range. Must be numeric and less than `max_val`.}

\item{max_val}{The maximum value in the scaled data range. Must be numeric and greater than `min_val`.}
}
\value{
A numeric vector of length `n` containing the generated data, scaled to the range [`min_val`, `max_val`],
and truncated to ensure all values fall within this range. Each value is rounded to 2 decimal places.
}
\description{
This function generates data following a left-tailed distribution by inverting a log-normal distribution,
then scales and truncates it to fit within a specified range. The data is rounded to 2 decimal places.
}
\examples{
generated_data <- generate_left_tail_data_ln(n = 100, mean = 50, sd = 10, min_val = 20, max_val = 100)
hist(generated_data)

}
